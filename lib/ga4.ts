/**
 * GA4 integration was replaced by the self-hosted analytics_events table
 * (lib/ga4.ts → /api/analytics/event + /api/analytics/summary).
 *
 * This file is intentionally empty so old imports compile if anything
 * still references it. Delete this file once you're sure nothing imports it.
 */
export {};
